version: "3.9"  # optional since v1.27.0
services:
  data-streamer:
    build: .
    expose: 
      - "9000"
      - "9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - log:/usr/src/data-streamer/log/

volumes:
  log: 
